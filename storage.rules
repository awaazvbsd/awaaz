rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /audio_backups/{userId}/{allPaths=**} {
      allow read, write: if true;
      // Note: For production, we'd use:
      // allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
